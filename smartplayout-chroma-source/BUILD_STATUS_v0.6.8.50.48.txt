SMART PLAYOUT v0.6.8.50.48 BUILD STATUS

SOURCE VALIDATION COMPLETED IN THIS WORKSPACE
- 23 XAML/MSBuild XML files parsed successfully.
- 348 XAML event-handler bindings matched to code-behind methods.
- 51 C# files passed lexical delimiter validation.
- OutputRouteRegistration has one compatible construction site and one worker deserialization path.
- No generated bin/obj folders or obsolete Runtime Decoder/Encoder/DirectShow/DeckLink-FFmpeg clones are present in the source pack.
- Locked CG and Chroma reference images are included unchanged.

PENDING MACHINE-SPECIFIC FINAL GATE
This workspace is Linux and has no dotnet/MSBuild/Windows Desktop SDK, PowerShell, Wine,
or physical DeckLink hardware. A new Windows WPF executable cannot be truthfully compiled
or launched here. Do not treat the older PLAYOUT executable as the v0.6.8.50.48 build.

RUN ON WINDOWS 10/11 X64
1. Extract this source package to a short local path.
2. Ensure a complete matching FFmpeg 9 x64 family is available in the extracted tree or
   in a location searched by PREPARE_FFMPEG_RUNTIME.bat.
3. Run BUILD_FINAL_SETUP.bat.
4. The build stops on SDK, FFmpeg, DeckLink bridge, worker EXE, IPC, heartbeat, layout,
   runtime-family, or packaging failure.
5. On success, use DIST\SMART_PLAYOUT_v0_6_8_50_48_PORTABLE.zip and, when Inno Setup 6
   is installed, DIST\SMART_PLAYOUT_v0_6_8_50_48_SETUP.exe.
6. Keep BUILD_FINAL_v0.6.8.50.48.log and MODULE_WORKER_VERIFICATION.log for verification.

IMPORTANT OUTPUT-WORKER SAFETY
The separate Output Worker receives the exact FFmpeg command template generated by the
existing validated embedded writer, including display SAR, DVB PIDs/service metadata,
NIC binding, multiple destinations, SRT mode/latency/passphrase, and encoder tuning.
Embedded output remains the compatibility authority until the Windows worker route is
hardware-tested; this avoids silently replacing a working on-air path with an unverified one.
